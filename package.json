{
  "name": "hello-world-app",
  "version": "1.0.0",
  "description": "simple hello world Express and React application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "server": "nodemon server.js",
    "concurrently": "concurrently \"node server.js\" \"cd client && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "cors": "^2.8.5",
    "express": "^4.17.3"
  },
  "engines": {
    "node": "8.11.2"
    },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
